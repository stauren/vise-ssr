import SSRServer from '@/ssr-server';

export type { SSRServerConfig } from '@/ssr-server';
export type { InitOption } from '@/start-server';

export { default as startServer } from '@/start-server';
export default SSRServer;
